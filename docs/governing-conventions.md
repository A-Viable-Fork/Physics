# Governing conventions

This document carries three policies the whole grading structure inherits from Stage 0 forward. None of them is re-decided by a later stage; a later stage cites this document rather than re-litigating it. Each policy is traced to the recon report section that argued for it.

## 1. Checker identity

The pipeline's checkers are AI systems the operator orchestrates: Gemini DR, DeepSeek, ChatGPT arbitration, and Claude itself under the operator's session. The protocol's `independence` field on a checking record (`distinct-party` or `self`) is a declared value the gate trusts structurally (protocol spec Section 2.3); whether one orchestrated system is a distinct party from another, or from the operator, is a semantic-attenuation question the protocol explicitly leaves to the community layer (protocol spec Section 1, "whether a named checker exists, whether claimed independence is real... is out of scope and community-level"). This kernel is that community layer's standing answer:

- **Each external AI system is its own `checker_id`, with its own source footprint.** `checker:gemini-dr`, `checker:deepseek`, `checker:chatgpt-arb`, extendable as new systems enter the pipeline. A checker's footprint traces to the sources that inform it (the report it read, the corpus it was pointed at), never shared wholesale with another checker's footprint merely because the operator ran both. `checker:pk-dr` (Stage 3c Track 3) extends this for the single prompt-author, single research-tool session that produced all six PK-DR deep-research reports (`docs/research/README.md`): one new external system, its six reports resting on one shared synthetic footprint root (`corpus.pk-dr-research-session`), so two checking records citing different PK-DR reports correctly cap at `checked`, never lift to `independently-rechecked`, the same shared-ancestor discipline this section already states for any two audits tracing to one upstream report.
- **Cross-system checks declare `independence: distinct-party`.** Gemini checking a claim Claude produced, or DeepSeek checking a claim Gemini produced, is a distinct-party check: different pipeline, different failure modes, a real second look.
- **Same-pipeline checks declare `independence: self`.** Claude checking Claude's own output under the same operator session is a self check. It may still be useful (a second pass catches slips), but it does not contribute to a claim's own basis (protocol spec Section 2.3: "only a `distinct-party` check contributes to a claim's own basis"), and it does not feed the independence lift.
- **Two audits tracing to the same upstream report share a footprint and are not independent**, even if they run through different `checker_id`s. If Gemini and DeepSeek both audit the same prior Gemini report rather than the underlying primary sources, their footprints intersect at that report, and the source table's `rests_on` chains carry this: `footprintClosure` (`kernel/schema/tables.mjs`) computes the reflexive-transitive closure over `rests_on`, and the independence lift to `independently-rechecked` requires two `distinct-party` checks whose footprints do not collapse into one under that closure.
- **The operator is the semantic-attenuation layer.** The gate trusts `checker_id` and `independence` as declared fields; it does not itself adjudicate whether a declared distinct party is really distinct. That adjudication is the operator's, made once per checker relationship and then held to, not re-argued per claim. This document is the standing account of what the declarations mean, so the convention is legible policy rather than an implicit inflation of how many independent eyes actually looked.

## 2. The Lean policy: constitutive mode, not gate criterion

Lean is the constitutive mode of the existing grade lattice, not the gate's closing criterion (recon report Section 4). A derivation grounds at the settled-empirical tier through derivation-audit checking records: one distinct-party audit reaches `checked`, two with disjoint footprints reach `independently-rechecked` (Section 1). A Lean proof is the `closing_condition` on the residual gap between audited and proved; when a proof lands and compiles, a fork retypes the claim to the `theorem` kind at the constitutive floor. Settled-empirical and constitutive are incomparable modes by design (recon report Section 2 mapping table), and comparing them is a decline: an audited derivation and a formal proof are different kinds of settled, not ranks of one kind.

Making Lean the gate criterion ("grounds" = "compiles") was considered and rejected. It would floor essentially the entire corpus at `asserted`, erasing the audit-tier distinctions the trellis spent eighteen months earning, and it would invert the incentive against opening derivation claims at all. The current Lean state (compiling modules with structural placeholders, a rank-44 proof estimated at hundreds of manual lines, a decade-scale tag on the exhaust PDE) makes a compile-gate demand the corpus cannot yet meet. Formalization is pull-driven instead: every unproved derivation permanently displays its distance from the proof floor, and the characterized-gaps read is the formalization backlog, prioritized. Stage 1 does not re-decide this; it enters derivations at their audited tier and lets Lean lift them later.

## 3. The verdict-ladder policy: extensions only, never the gate

The DG trellis's six-step contract verdict ladder (Confirmed / Strongly Supported / Marginal / Suggestive / Plausible / Failed) does not become a local grade position. The earned-grade computation runs over the fixed confidence line (`kernel/schema/confidence.mjs`, `kernel/grounding/earned-grade.mjs`); the ladder is a domain judgment the gate deliberately does not adjudicate ("marginal" is a reading of how close a number sits to a threshold, not a grounding-structure fact). The compression is lossy by design and correctly so.

The verdict rides in a claim's `extensions` object: undeclared fields are canonicalized and hashed into the record (protocol spec Section 2) but read by no check. The gate never reads it; the client periphery renders it for a human reading the graph. Minting local grade names for the ladder positions, though permitted as policy, would not enter the fold the gate computes over, and this kernel does not do it. Stage 1 enters domain verdicts in `extensions` from the first claim that carries one, not as a later migration.

## 4. `peer-reviewed` is external literature only

`source_class: "peer-reviewed"` marks a document that has actually been through human review at a venue: a journal, a conference proceedings, a body with an editorial or refereeing process external to this corpus's own generation. No corpus-internal document takes this class until human review has actually occurred, regardless of what its own title claims. `corpus.peer-review` (Stage 2's "Peer Review Report: State-Dependent Vacuum Geometry") is not peer-reviewed by this definition: no document in this corpus has been seen by a human reviewer, so a document's self-description as a "peer review" does not govern its class. It is an adversarial audit of the framework in the same genre as the corpus's other AI-system audit reports and is classed `ai-audit` (Stage 3a Track 0, operator ruling 2). The test for a future entry is mechanical: is there a named external venue with an actual review process, distinct from this project's own AI-assisted generation pipeline? If yes, `peer-reviewed`. If the document is itself a pipeline product, whatever its title says, it is not.

## 5. The independence-lift policy for `rests_on` lineage groups

An `independently-rechecked` lift (two `distinct-party` checking records whose footprints do not collapse into one under `footprintClosure`) may cite, as a disjoint-footprint pair, only sources whose lineage is affirmatively known: a source in the **root** group (no earlier corpus document to rest on, confirmed by a close read) or the **parallel-independent** group (two or more investigations of the same question confirmed to proceed by different pathways with no citation of each other). Both are affirmative determinations, made by reading the text, not absence of a link.

A source whose `rests_on` lineage is **unknown** (no determinable predecessor found, listed as a gap rather than defaulted, per `docs/status-ledger.md`'s Stage 2.5 Track 3 gap list) caps any lift through it at `checked`, never `independently-rechecked`, until its lineage is resolved. Unknown is never read as independent: a footprint that has not been traced might collapse into another footprint once traced, and the gate's independence lift must not assume the favorable case merely because the unfavorable one has not been demonstrated. This is the same discipline the lineage gap list itself follows (a visible gap over a guessed link), applied one layer up to what the gap list is then permitted to certify.

**Amended at Stage 3b (`docs/synthesis-backlog.md` 2.1): independence is computed, not declared.** Two checking records are independent if and only if their source footprints, taken as ancestor closures over `rests_on`, have empty intersection: exact set membership, not a proxy for it. This supersedes any reading of this section as vendor-disjointness (different `checker_id` alone) or method-disjointness (different `method_class` alone) being sufficient; the correlated-errors finding behind the amendment is that neither a different vendor nor a different method guarantees a different evidentiary footprint. `independence: distinct-party` on a checking record remains meaningful as checker-identity semantics (Section 1: a different pipeline, a real second look by a different system) but no longer carries independence on its own; it is necessary, never sufficient. The unknown-group rule above stands unchanged and is now permanent: unknown lineage caps a lift at `checked` regardless of how many distinct-party records exist.

A substrate note, load-bearing for how this is actually verified: `kernel/schema/records.mjs`'s `checkingRecord()` builder canonicalizes only its declared fields (`checker_id`, `method_class`, `method`, `checked_at_state`, `outcome`, `independence`) onto the object it returns; any other field passed in, including a hand-authored `footprint`, is folded into `canonical.extensions` (hashed, tamper-evident, per the record's own design) and is not present as a top-level property on the object the gate later reads. `kernel/grounding/earned-grade.mjs`'s `ownBasis()` reads `checkingRecords[i].footprint` directly (a bare property, not `.canonical.extensions.footprint`), so for a real, schema-built checking record this is always `undefined`, and `disjoint(undefined, undefined)` is vacuously true. The practical consequence: the real gate's own automatic independence lift cannot be trusted to enforce the disjoint-footprint requirement against schema-validated records as this substrate constructs them; it will grant the lift whenever two `distinct-party` records exist, independent of whether their true footprints actually overlap. This build does not patch the vendored builder (the trust boundary stays one-directional); it computes independence itself, externally, against the raw authored data (`footprintClosure` over each checking record's actual source, read from the claim's pre-schema `spec`, not the post-schema `rec`), and declares each claim's own `declared_grade` at the conservative, externally-verified value, never the value the gate's own (over-permissive, for this reason) computation would allow. `GM-ABOVE` only blocks declaring above earned, never below, so under-declaring the honest way costs nothing and the gate's leniency here is never exploited. `build/check-independence.mjs` (Stage 3b) re-verifies this discipline against the real built corpus on every run, which is the actual enforcement layer for this policy; this document states why that check exists rather than trusting a declared-versus-earned comparison alone.

## 6. Ordinal discipline on the grade lattice

Amended at Stage 3b (synthesis backlog 2.2). Valid operations on a grade or a set of grades are `meet`, `join`, and monotone step functions over the fixed confidence order (`kernel/schema/confidence.mjs`) only. No averaging, no weighted sums, no compensation-style aggregation (a strong claim does not offset a weak one), and no cross-mode aggregation: the settled tier's two modes (empirical, constitutive) are incomparable by design and are processed component-wise, never combined into one cardinal scale. This was already the vendored gate's behavior (`earnedGrade`, `supportDelivery` use only `meet`/`join`/`capByCeiling`); the amendment makes it standing policy for every future tool this kernel builds or adopts, physics-specific or general. A future proposal that assumes cardinal-valued standing, a probability, a weight, or a score over grades is refused at design time, not merely declined case by case; the six-report synthesis names this the sorting rule for all future tooling (`docs/synthesis-backlog.md` Section 1: independence, cardinality, and thermodynamic-limit assumptions are presumptively dead here until shown otherwise).

## 7. Contradictions resolve by crux, never by smoothing

Amended at Stage 3b (synthesis backlog 2.3). A held `contradicts` link (Path A versus Path B is this kernel's standing example) is never resolved by statistical reconciliation, weighted averaging, or any method that treats the two sides as noisy estimates of one quantity: `docs/synthesis-backlog.md` Section 3, kill K1 retires PDG-style scale-factor averaging over contradictory claims for exactly this reason, and the kill enters the reservoir at Stage 3c. The kernel-native synthesis over a held contradiction is the computed crux: the symmetric difference of the two branches' minimal supporting environments, the exact set of foundational commitments the two sides do not share. This is a structural computation over the provenance graph (an environments engine, `docs/synthesis-backlog.md` Section 4.1, is the general form; the Path A/Path B case is small enough to read by hand until that engine exists), never a numeric compromise between the two positions. `decide()`'s existing `accepted-with-disagreement` reading for a `contradicts` link is the gate-level expression of this: a contradiction is registered and both sides stand, not merged into one blended verdict.

Resolution of an unknown lineage group is lazy: done per pair, at the moment a lift actually needs it, never as a bulk pass over the whole gap list. A bulk pass would spend effort resolving lineage no lift will ever read, and would tempt the same guessing the gap list was built to avoid. `check-independence.mjs` (Stage 3b) mechanizes this policy against the real `rests_on` data and the real lineage groups recorded in the ledger; this document states the rule, Stage 3b implements it.

## 8. Reinstatement-condition typing for the reservoir

Set at Stage 3c Track 1. The protocol's `reinstatement_condition` on a withdrawn-claim record (`kernel/schema/records.mjs`, `CONDITION_KINDS`) admits exactly three typed kinds: `entry-of-kind`, `entry-at-grade`, `citation-into-class`. The trellis's own reservoir prose states reinstatement conditions in free text ("discovery that X," "structurally none," "not contemplated"), which does not map onto these three kinds one-for-one. This kernel's standing policy for the mismatch:

- **A categorical kill** ("structurally none," "not contemplated," no candidate named) is typed `entry-at-grade`, with `target_identity` set to the withdrawn claim's own identity and `minimum_grade` set one rank above what the claim's own kind ceiling can ever deliver (`"checked"` against a `mechanism`/`function` ceilinged at `corroborated`, a settled-tier grade a non-settled-tier ceiling structurally cannot reach). This is a real, mechanically permanent decline computed by the actual gate machinery, not a guessed sentinel value standing in for "never": `capByCeiling` (`kernel/schema/confidence.mjs`) enforces the ceiling on every earned-grade computation, so no future checking record or support, however strong, can move the claim past it.
- **A named future discovery or demonstration** ("discovery of a mechanism not yet identified," "a reformulation that would...") is typed `entry-of-kind` with `required_kind: "derivation"`, the nearest available typed approximation. The schema's `entry-of-kind` condition only asks whether *some* claim of the required kind exists; it cannot express the specific content the trellis names ("and it must show the measure factor is wrong," "and it must derive a healthy-sign kinetic term"). This is a real gap, not smoothed over: every withdrawal record typed this way carries the elided content verbatim in its own `reason` field, so a future claim satisfying the typed condition mechanically is not mistaken for one that has actually met the trellis's real bar. `docs/status-ledger.md`'s Stage 3c section names this gap for every affected entry, per the standing discipline (Section on contradictions, above: gaps are ledgered, never guessed past).
- **A condition naming a real, already-entered claim's future grade** would be typed `entry-at-grade` against that claim's actual identity; none of the reservoir's own reinstatement text happens to fit this shape (several name an existing claim being *shown wrong*, the inverse of `entry-at-grade`'s "reaching a minimum grade," which the schema cannot express forward), so this path is documented but not exercised by Stage 3c's actual data.

A withdrawal's `reinstatement_condition` and a restatement link naming the withdrawn identity are independent gate mechanisms (`WD-UNSATISFIED` and `WF-SUPERSEDED` respectively, `vendor/kernel/gate/gate.mjs`): the first asks whether reinstatement's own bar is met, the second declines any reference at all to an out-of-force identity, naming the successor. Genuine reinstatement is therefore a fresh entry that does not restatement-link back to the dead claim; verified in `docs/status-ledger.md`'s Stage 3c verification exercises.

## 9. Weakest-of non-monotonicity: two senses, classified

Set at audit-prep Track 0.2, following up analysis-1's own k10 verification exercise (`analysis/environments.mjs`'s minimality test, docs/status-ledger.md Analysis-1 section). "A claim's earned grade did something counter-intuitive when something upstream changed" collapses two genuinely different phenomena that must not be conflated:

- **Structure-sense (by design, not a bug).** Removing a `support_group` member (severing a link, not touching any grade) can raise a claim's earned grade, since `supportDelivery`'s weakest-of fold (`kernel/grounding/earned-grade.mjs`) runs over whichever links currently exist, not a fixed arity: a meet computed over fewer terms is less constrained. The inverse (adding a weak conjunctive requirement lowers standing until that requirement itself grounds) is the same fact read the other direction. Documented in `analysis/environments.mjs`'s own header comment; a minimal environment names every member the current structure genuinely requires, whether or not that member currently helps the numeric grade.
- **Valuation-sense (investigated, not reproduced).** The distinct, more alarming-sounding claim would be: holding the support structure fixed and elevating one leaf's own earned grade lowers a downstream claim's earned grade. Audit-prep Track 0.2 searched this directly against the real substrate: 432 fixed-structure scenarios (every combination of kind ceiling, own-basis presence, AND-group and OR-group shape, and fixed co-member grade, with one leaf elevated across the full empirical chain `ungraded < asserted < supported < corroborated < checked < independently-rechecked` in each) produced zero violations. This is not merely an empirical negative; it follows from the kernel's own GROUNDED lattice proofs (`thm.meet-*`, `thm.join-*`, `kernel/schema/confidence.mjs`, proved by `build/check-math-exhaustion.mjs` in the upstream substrate): `meet`/`join` are monotonic by construction, `capByCeiling` is monotonic in its first argument, and the two-row switch (`Bsettled && (S >= corroborated || noSupports)`) always selects the *stronger* of its two available options when it fires, since a settled own basis is never weaker than the corroborated-or-below support delivery it displaces. Recorded here, alongside the footprint-drop bug (Section 5), as a checked-and-cleared item, not a defect: the planned Shapley/Banzhaf sensitivity read (`docs/synthesis-backlog.md` Section 4.3) assumes leaf-monotonicity for its own correctness, and this investigation is that assumption's verification, not its discovery as a problem. The substrate is not patched either way; there was nothing to patch.

**One-paragraph note for whoever builds the Shapley read**, in place of an upstream bug report (since no bug was confirmed): leaf-monotonicity of `earnedGrade()` under fixed support structure was checked exhaustively across ceiling, own-basis, and AND/OR-shape combinations and held in every case; the two-row settled-not-inherited switch, which looks like the most likely place monotonicity could break, always resolves toward the stronger option. This clears the ground assumption a Shapley computation over `supportDelivery`'s provenance polynomial would need, but the search did not cover three-or-more-group interactions, restatement-closure merges (no real restatement link exists in this corpus to test against), or the footprint-drop bug's interaction with a fresh elevation; a Shapley read that turns out to disagree with this ground assumption should replant this same search (vary one leaf's own earned grade across the empirical chain against `earnedGrade()` directly, holding `checkingRecords`/`supports` structure fixed, per Track 0.2's method above) against its own specific construction before assuming a new defect.

## 10. The import discipline (landing-1)

Set at landing-1 Track 0, for the first session that cites canonical published literature directly rather than through the trellis's own generic citation or an AI-audit pipeline document.

**Documents are provenance; imported claims are epistemics.** Evidence for content never cites a document directly; it cites an imported claim: a claim whose statement is the specific proposition actually used, whose kind is an existing import kind (`standard-result`, `observation`, `conjecture-adopted`), whose grade is honest, and whose `extensions` carries a **locator** (document, section, equation or theorem number naming exactly where in the source the proposition is found). A source row still exists and still carries the document's own provenance (`source_class`, `rests_on`); the import claim is the graph-native object that makes the specific proposition citable, checkable, and supersedable on its own, the same way any other claim is. Existing coarse citations (a bare `source_id` naming a document, no imported claim between it and what rests on it) are grandfathered and migrate opportunistically, continuing the direction audit-prep Track 1's granularity pass already moved in; every new evidence claim entered from this session forward follows the discipline from the start.

**The `departure` kind.** Registered in `corpora/dg-config.json`'s `local_kinds` (ceiling `corroborated`) and mirrored by hand into the generated `corpora/dg/tables.js`, since re-running `build/new-kernel.mjs` would reset `corpora/dg/dg-data.js`'s STORE to an empty skeleton and destroy the corpus; the config is the durable record of the revision, following the same discipline the vendored substrate's own `docs/schema-revisions.md` uses for its schema (name what forced the change, apply it, record it at the point of change), scaled down to a one-line kind addition. A `departure` claim states where the framework uses, but knowingly departs from, an imported result: it is supported by two things, the imported claim it departs from and the framework's own replacement, and it carries the departed-from residue as a `closing_condition` where one exists (`vendor/kernel/analysis/characterized-gaps.mjs`'s existing read picks it up automatically, the same read that already tracks the sorry ledger's `derivation`-kind gaps). Departures render via a pure read, `build/emit-departures.mjs`, sibling to `build/emit-nogo-registry.mjs`.

**The identity-collision hazard.** Audit-prep Track 1 found that `computeClaimIdentity(kind, statement)` (`vendor/kernel/schema/records.mjs` `claimRecord()`) hashes only `kind` plus `statement`, never `source_id`, `checking_records`, or `declared_grade`; a re-citation or supersession attempt that keeps the new claim's `statement` byte-identical to the superseded claim's produces an identical identity hash for both, silently corrupting every identity-keyed structure downstream (`api/dg-provider.mjs`, `analysis/environments.mjs`), documented only in that session's own commits until now. Added here as a known substrate issue alongside the footprint-drop bug (Section 5) and treated as live in every session touching this store from landing-1 forward: before entering any import claim or a claim intended to supersede an existing one, verify its `(kind, statement)` pair collides with nothing already in the corpus; where a deliberate near-duplicate statement is genuinely needed (the re-citation case), differentiate the statement text honestly, with real substantive content the identity hash can see, rather than relying on any field the hash cannot see. A collision encountered during authoring is reported in the session's own commit or ledger entry, never silently worked around.

**Canonical roots (R3).** Imported claims resting on canonical published literature are legitimate shared roots that do not, on their own, contaminate audit independence; shared pipeline documents (this corpus's own AI-audit, preprint, and testimony sources) still do, unchanged. A source is a **canonical root** iff it is itself class `peer-reviewed` and its entire `rests_on` lineage (its own closure under `footprintClosure`, including itself) is `peer-reviewed` throughout: a genuinely external-literature citation that never rests on a pipeline document. `build/check-independence.mjs` computes the ancestry intersection between two checking records' footprints with canonical-root nodes excluded from both sides before the intersection test, so two records that share only a canonical root (two audits both citing Israel 1966's junction condition, say) test as independent, while two records sharing an `ai-audit`, `preprint`, or `testimony` ancestor are still blocked exactly as before. Exercised in scratch, both directions: two synthetic records sharing only a canonical peer-reviewed root pass (the shared ancestor is filtered from both sides, leaving disjoint empty sets); two records sharing an `ai-audit` ancestor still fail (the shared ancestor is not a canonical root and is not filtered, so the sets still intersect).
